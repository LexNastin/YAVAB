const Discord = require('discord.js');
require('dotenv').config();

module.exports = class CMD_NAME {
	constructor() {
		this.name = '',
		this.alias = [''],
		this.descr = '',
		this.usage = ``
	}

	async run(client, message, args) {
		
	}
}